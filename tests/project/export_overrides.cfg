; Exercises the checker's `export-overrides` rule, which `check-plugin` runs over this
; project on every pull request. See plugins/godot/checker/README.md.

[*]

export_filter="all_resources"
exclude_filter=["*/editor/*"]

[*-macos-*]

options/application/bundle_identifier="com.coffeebeats.godot-infra"
