; Exercises `export-overrides` and `export-ref` over this case's presets.

[*]

exclude_filter=["*/editor/*", "no-such-directory", "*.nothing"]

[*-macos-*]

options/application/bundle_identifier="com.example.declared"
options/binary_format/architecture=1
options/no_such/option=true

[*-linux-*]

export_filter="all_resources"
include_filter="*.txt"
