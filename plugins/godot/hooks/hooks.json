{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|apply_patch",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/on_edit.py\" || uv run --no-project --quiet python \"${CLAUDE_PLUGIN_ROOT}/hooks/on_edit.py\""
          }
        ]
      }
    ]
  }
}
